# Runtime dependencies, pinned against the versions actually resolved and
# imported on this machine rather than from memory.
#
# Install with the constraints file matching the local Python version:
#   python -m venv .venv
#   .venv/bin/pip install -r requirements.txt -c constraints-3.12.txt
#
# ADK 2.8.0 is the current 2.x release. The hard constraint is the 2.x line:
# 2.0 introduced breaking changes to the agent API, event model, and session
# schema, so no 1.x pattern is valid here regardless of the minor version.
google-adk==2.8.0

google-cloud-firestore==2.29.0
google-cloud-pubsub==2.39.2
google-cloud-logging==3.16.3
google-cloud-trace==1.20.0
python-dotenv==1.2.3

# --- Development ---
pytest==9.1.1

# Cloud Trace export, used by ADK's get_gcp_exporters helper.
opentelemetry-exporter-gcp-trace==1.15.0
